# Backend - Fábrica de Maniquíes

## 🚀 Instalación
1. Clonar el repositorio
2. Instalar dependencias:
   ```bash
   npm install


Crear un archivo .env en la raíz del backend con tus credenciales:
PORT=3000
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=root
DB_PASSWORD=tu_clave
DB_NAME=fabrica_maniquies

Abrir phpMyAdmin o tu cliente MySQL.

mysql -u root -p < backend/db/create_and_inserts.sql




Frontend

### `npm start`

Runs the app in the development mode.\
Open [http://localhost:4000] to view it in your browser.


backend

### `node index.js`

Open [http://localhost:3000]
